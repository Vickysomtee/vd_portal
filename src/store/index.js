import { createStore } from "vuex";
import auth from "@/store/auth";
import Api from "../Api";

export default createStore({
	state: {

	},
	mutations: {

	},

	actions: {

	},

	modules: {
		auth,
    }
});